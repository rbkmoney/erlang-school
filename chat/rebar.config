{erl_opts,
 [debug_info,
  warnings_as_error,
  {parse_transform, lager_transform}
  ]
}.

{deps,
  [{lager, "3.2.1"},
   {cowboy,"1.1.2"},
   {jiffy, "0.15.2"} % Needed for not yet added to git module
  ]
}.

{profiles,
  [
   {lint,  [{plugins, [{rebar3_lint, {git, "https://github.com/project-fifo/rebar3_lint.git", {tag, "0.1.2"}}}]}]}
  ]
}.

{shell,
  [{apps, [chat]}]
}.
