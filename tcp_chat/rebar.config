{erl_opts,
    [debug_info,
     warnings_as_error,
     {parse_transform, lager_transform}
     ]}.

{deps,
    [{lager,"3.2.1"}
    ]}.

{shell, [
  % {config, [{config, "config/sys.config"}]},
    {apps, [tcp_chat]}
]}.
